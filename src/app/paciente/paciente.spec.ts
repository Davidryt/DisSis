import { paciente } from './paciente';

describe('paciente', () => {
  it('should create an instance', () => {
    expect(new paciente()).toBeTruthy(); //Lo vi en internet, pero no tengo claro como va
  });
});
