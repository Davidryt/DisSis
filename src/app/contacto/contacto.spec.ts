import { Contacto } from './contacto';

describe('Contacto', () => {
  it('should create an instance', () => {
    expect(new Contacto()).toBeTruthy();  //Lo vi en internet, pero no tengo claro como va
  });
});
